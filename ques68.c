#include <stdio.h>

void main() {

    int a = 2;
    int b = a;

    switch(b)
        {
            case a;
                printf("Case-a \n"); break;

            case 3;
                printf("No option \n"); break;

            default:
                printf("Exit from switch");
        }

    printf("Exit from switch");

    return 0;
}